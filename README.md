# skilldirectorynativeui

Native implementation of the frontend.

#### To Run
1. `npm install -g react-native-cli`
1. Install XCode
1. `react-native run-ios`
1. To get debug logging: `react-native log-ios`
